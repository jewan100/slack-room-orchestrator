# AGENTS.md

이 문서는 `slack-room-orchestrator` 레포에서 작업하는 모든 AI 워커(Codex 포함)의 **작업 계약서**다.

목표: 누구(어떤 워커)가 작업하더라도, 동일한 기준/문서/품질로 개발·리뷰·PR을 수행하게 한다.

---

## 0) 우선 원칙

1. 이 레포는 단기 실험이 아니라 **장기 확장형 오케스트레이션 베이스**다.
2. 규칙 충돌 시 문서 우선순위를 따른다(아래 1장).
3. 불명확한 경우 임의 추정 금지. PR 본문에 가정/리스크를 명시한다.
4. 취향보다 안정성/일관성/추적성을 우선한다.

---

## 1) 문서 우선순위 (반드시 이 순서로 참고)

워커는 작업 시작 전 아래 순서대로 문서를 읽고 판단한다.

1. `AGENTS.md` (AI 워커 작업 계약, 최상위 작업 지침)
2. `JEWAN_DEV_CONSTITUTION.md` (전역 개발 헌법, 최상위 원칙)
3. `STANDARDS.md` (레포 공통 구현/프로세스 표준)
4. `SLACK_COMMAND_ENGINEERING_STANDARDS.md` (도메인 특화 규칙)
5. `README.md` (현재 레포 운영 맥락/확장 방향)
6. `docs/decision/DECISION_LOG.md` (현재 유효 기준선)
7. `docs/reviewer/PR_REVIEW_POLICY.md` (리뷰 절차)
8. `docs/reviewer/CODEX_FINAL_REVIEW_CHECKLIST.md` (최종 점검 항목)
9. `.github/copilot-instructions.md` (Copilot 작업 가이드)
10. `.github/codex-instructions.md` (Codex 최종 게이트 기준)
11. `.github/PULL_REQUEST_TEMPLATE.md` (PR 작성 형식)

### 충돌 처리 규칙
- 문서 간 충돌 시: `CONSTITUTION > STANDARDS > 도메인 규칙 > 기타 문서`
- 제품/스펙/범위 결정 충돌은 `docs/decision/DECISION_LOG.md`를 우선한다.
- 충돌 발견 시 반드시 PR에 “문서 충돌”로 명시하고 정리 커밋 포함

---

## 2) 개발 방법론 (Development Doctrine)

### 2.1 아키텍처/구조 원칙
- 레이어/경계 분리를 유지한다.
- 외부 연동은 adapter 경계로 분리한다.
- SRP(단일 책임) 위반을 만들지 않는다.

### 2.2 코드 스타일 원칙
- 코드 스타일 단일 원본: `JEWAN_DEV_CONSTITUTION.md`
- 핵심 키워드: 가독성/명시성, 네이밍 엄격, SRP, validator 분리, 독자 친화형+맥락형 주석, 이식성/확장성
- 강제 규칙: `any` 금지, `latest` 금지, 민감정보 로그 금지
- 문구 규칙: 사용자 노출 문구/에러 문구는 중앙 카탈로그에서 관리
- 주석 문체 규칙: 동사 문장만 `~다.` 사용, 명사 문장은 `~다.` 미사용
- 상세 수치 기준/예외 처리 규칙은 `JEWAN_DEV_CONSTITUTION.md`를 따른다.

### 2.3 브랜치 전략
- 기본 브랜치: `develop`
- 기능 브랜치: `feat/` prefix
- 브랜치명 형식: `prefix/actual-name-vN`
- `actual-name`: 영어 소문자 + 하이픈
- `vN`: 같은 기능의 반복 작업 차수를 의미 (`v1`, `v2`, `v3` ...)
- 예: `feat/add-slack-reminder-v1`, `feat/add-slack-reminder-v2`
- 날짜/이슈번호는 현재 필수 아님
- 강제: `develop`에서 직접 작업/커밋/푸시 금지
- 강제: 모든 작업은 기능 브랜치에서 시작하고 PR로만 반영
- 강제: 커밋/푸시 전에는 반드시 사용자에게 확인을 받는다.

### 2.4 변경 단위
- 한 PR은 한 목적에 집중한다.
- 문서/코드/리팩토링이 섞이면 PR 본문에 범위를 분리 기재한다.

### 2.5 코딩 에이전트 4원칙 (Karpathy Guidelines)

아래 4원칙은 모든 AI 워커의 기본 행동 원칙이다.  
상세 코드 스타일 규칙은 `JEWAN_DEV_CONSTITUTION.md`를 단일 원본으로 따른다.

- **Think Before Coding (코드 작성 전에 생각)**
  - 목표/성공 기준/비목표를 1문장으로 고정한다.
  - 영향 범위(파일/기능/운영)를 먼저 식별하고, 불확실하면 질문으로 드러낸다.
  - 구현 전에 “어떤 변경을 왜 하는지”를 단계형 체크리스트로 정리한다.
  - 임의 추정 금지, 가정/리스크는 PR 본문에 명시한다.

- **Simplicity First (단순함 우선)**
  - 기존 레이어/경계(adapter/service/repository/validator)를 유지하는 범위에서 가장 단순한 해법을 우선한다.
  - 새 추상화/새 패턴/새 의존성 도입은 “필요성 + 대안 + 트레이드오프” 문서화 없이 금지한다. (필요 시 ADR)
  - “미래 확장 대비”를 이유로 현재 요구사항을 넘는 구현을 하지 않는다.

- **Surgical Changes (외과수술식 변경)**
  - 한 PR은 한 목적, 목적과 무관한 리팩토링/정리/포맷 변경을 금지한다.
  - 불가피한 대규모 변경은 근거 문서화 + 범위 분리(PR 또는 커밋) + 영향 범위 명시를 강제한다.
  - 변경은 최소 diff로 유지하고, 동작/계약 변화는 명시적으로 드러낸다.

- **Goal-Driven Execution (목표 지향 실행)**
  - “완료 조건(DoD)”을 PR 템플릿의 검증/영향 섹션에 근거로 남긴다.
  - 필요한 검증(`lint`/`typecheck`/`test` 또는 대체 근거)을 제시하지 못하면 “완료”로 간주하지 않는다.
  - 문서/결정/후속 TODO까지 연결되어 추적 가능해야 한다.

---

## 3) PR 작성 규칙 (모든 워커 공통)

PR 작성 시 `.github/PULL_REQUEST_TEMPLATE.md`를 따른다.

반드시 포함할 것:
1. 왜 이 PR이 필요한지
2. 무엇이 바뀌었는지
3. 영향 범위
4. 확인/주의사항(필요 시)

표기 규칙:
- 파일/경로/명령어는 코드 포맷으로 표기
  - 예: `README.md`, `docs/reviewer/...`, `npm run test`

금지:
- 근거 없는 과장 표현
- 변경 영향 누락
- “테스트함” 같은 증거 없는 서술

---

## 4) 리뷰 운영 규칙

### 4.1 2단계 리뷰
1. 1차: Copilot (빠른 피드백)
2. 최종: Codex (머지 직전 Final Gate)

### 4.2 Codex 최종 리뷰 기준
- `.github/codex-instructions.md` + `docs/reviewer/CODEX_FINAL_REVIEW_CHECKLIST.md` 기준으로 판정
- Verdict는 `PASS | CONDITIONAL | BLOCK` 중 하나로 명시
- 최종 2차 검토 호출 코멘트는 아래 문구를 그대로 사용:
  - `@codex 최종 2차 검토 요청. 기준: AGENTS.md, STANDARDS.md, .github/codex-instructions.md`

### 4.3 머지 조건
- Blocking 이슈 없음
- 필수 수정사항 반영 완료
- 문서/규칙 모순 없음
- (코드 변경 PR일 경우) 필요한 검증 단계 통과

---

## 5) 문서 유지보수 규칙

워커는 아래를 항상 점검한다:
- `README.md` 요약과 실제 규칙 문서 간 불일치 여부
- 도메인 규칙 문서명/경로 변경 시 참조 링크 동시 업데이트
- 하드코딩된 레포명/환경명이 재사용성을 해치지 않는지

문서 개편 시 원칙:
- 핵심은 `README.md`에서 빠르게 파악 가능해야 함
- 상세 규칙은 전용 문서로 분리
- 문서가 많아져도 참조 우선순위는 AGENTS 기준으로 고정

---

## 6) 워커 실행 체크리스트 (작업 시작 전)

- [ ] 문서 우선순위 순서대로 최신 확인
- [ ] 이번 변경의 목적 1문장 정의
- [ ] 영향 범위(파일/기능/운영) 사전 식별
- [ ] 성공 기준/비목표/가정을 먼저 정리했는가(Think Before Coding)
- [ ] 브랜치명 규칙 준수 확인

## 7) 워커 제출 체크리스트 (PR 전)

- [ ] 변경 내용이 목적과 일치
- [ ] 목적과 무관한 변경이 섞이지 않았는가(Surgical Changes)
- [ ] 파일/경로/명령어 코드 표기 적용
- [ ] 영향 범위/주의사항 PR에 기재
- [ ] 규칙 충돌/예외 발생 시 명시

---

## 8) 이 레포에서 자주 틀리는 포인트 (주의)

1. `feat/*`와 “하이픈 규칙” 충돌처럼 보이는 문구
   - 해결: prefix 슬래시는 허용, 실제 이름은 하이픈 규칙 + `-vN` 패턴 적용
2. 문서 필수/권장 혼동
   - 해결: 필수/권장을 구분해 명시
3. PR이 체크리스트 과다로 장황해짐
   - 해결: 배경/변경/영향 중심으로 간결 작성
4. README와 상세 문서 간 미세한 문구 불일치
   - 해결: 변경 시 양쪽 동시 업데이트

---

## 9) 최종 원칙

- 워커는 “빨리”보다 “일관되게”를 우선한다.
- 이번 PR 하나가 아니라, 다음 100개의 PR 품질을 만든다는 관점으로 작업한다.
