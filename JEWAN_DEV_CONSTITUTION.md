# JEWAN_DEV_CONSTITUTION.md

> 목적: 프로젝트 종류와 기술 스택이 바뀌어도 동일하게 적용되는 전역 개발 헌법.
> 원칙: 가독성, 추적성, 이식성, 확장성, 단일 책임.
> 단일 원본: 코드 스타일 상세 규칙은 이 문서를 단일 소스로 사용한다.

---

## 0) 절대 원칙 (Non-negotiable)

1. **가독성 최우선**: 짧은 코드보다 읽기 쉬운 코드 우선.
2. **명시성 우선**: 파라미터/데이터 출처가 코드에서 즉시 보여야 함.
3. **SRP 강제**: 함수/클래스/모듈은 하나의 책임만 가짐.
4. **이식성 보장**: 환경이 바뀌어도 동작 동일성 유지.
5. **추적성 보장**: 모든 변경/결정/보류사항은 기록.
6. **확장성 우선 설계**: 외부 기술 교체 가능 구조 유지.

---

## 1) 코드 스타일 규칙 (단일 원본)

### 1.1 가독성/포맷팅
- 들쑥날쑥한 depth 금지 (중첩 최소화)
- 코드 정렬/줄바꿈/공백을 미적으로 일관되게 유지
- 한 눈에 흐름이 읽히는 구조 우선

### 1.2 제어 흐름
- Guard clause 우선
- `else` 최소화
- 긴 분기/반복은 함수 분리
- 불필요한 조건 중첩 금지

### 1.3 네이밍 (엄격)
- 이름만 보고 역할/의도가 즉시 드러나야 함
- 함수명은 동사 시작, 도메인 용어 일관성 유지
- 약어 남용 금지
- 네이밍 규칙 위반은 PR 차단

### 1.4 SRP/역할 분리
- 함수 1개 = 책임 1개
- 서비스는 orchestration 책임만
- 유효성 검증은 validator 계층으로 분리
- 공통 검증 로직은 재사용 가능 컴포넌트로 관리

### 1.5 타입 안정성/인터페이스 명시성
- `any` 금지
- 입출력 타입 명시
- null/undefined/예외 흐름 명시적 처리
- 외부 노출 함수/메서드는 반환 타입 명시

### 1.6 상수/중복
- 매직 넘버/스트링 상수화
- 중복 로직은 공통 모듈로 추출

### 1.7 엄격-실무형 수치 기준
- 함수 길이: 최대 50줄
- 파일 길이: 최대 400줄
- 함수 인자 수: 최대 4개
- 순환 복잡도: 최대 10
- 중첩 깊이: 최대 3

### 1.8 주석 정책 (독자 친화형 + 맥락형)
- 원칙: 코드를 처음 읽는 사람도 추가 설명 없이 흐름을 이해할 수 있어야 한다.
- 기본: 모듈 역할, 핵심 함수 책임, 입출력 의미, 실패 조건을 주석으로 설명한다.
- 상세: 비자명 의도/함정/트레이드오프는 반드시 주석으로 남긴다.
- 균형: 자명한 동작을 줄 단위로 반복 설명하는 주석은 지양하되, 독해 난이도가 높은 구간은 충분히 상세하게 작성한다.
- 작성 방식: "왜(why) + 어떤 맥락에서(when) + 어떤 영향이 있는지(impact)" 중심으로 작성한다.
- 언어 기준: 주석은 한글 우선을 원칙으로 한다.
- 문구 관리: 사용자 노출 문구/에러 문구/운영 안내 문구는 중앙 카탈로그에서 일괄 관리한다.
- 상세도 기준:
  - 파일/모듈 상단: 이 파일이 해결하는 문제와 책임 범위를 설명한다.
  - 핵심 함수: 처리 순서를 단계로 설명하고, 호출 목적을 남긴다.
  - `await`/외부 I/O/상태 전이/DB write-read 구간: "무엇을 기다리는지, 왜 필요한지, 결과를 어디에 쓰는지"를 설명한다.
  - 예외/실패 분기: 어떤 조건에서 어떤 에러코드(또는 실패 경로)로 이어지는지 설명한다.
- 문체 기준:
  - 동사로 끝나는 설명 문장만 `~다.`를 사용한다. (예: `조회한다.`, `검증한다.`)
  - 명사형 설명 문장에는 `~다.`를 붙이지 않는다. (예: `런타임 설정 모델`, `요청 컨텍스트`)
- 테스트 코드 기준:
  - 테스트 코드는 본문 코드보다 주석 밀도를 낮게 유지할 수 있다.
  - 다만 의도가 불분명한 fixture/경계 케이스는 주석으로 맥락을 남긴다.

### 1.9 비동기/에러 처리 스타일
- floating promise 금지 (`await` 또는 명시적 무시 처리)
- 도메인 에러코드 기반으로 예외를 매핑하고 응답 형식 일관성 유지
- 예외를 삼키지 말고 로그 컨텍스트와 함께 전달

### 1.10 불변성 우선
- 재할당이 필요 없는 값은 `const` 사용
- 상태 변경은 경계 계층에서만 수행하고, 도메인 계층은 불변성 우선

### 1.11 예외 처리 원칙
- 불가피한 예외는 근거를 문서화하고 PR에 범위/리스크를 명시
- 예외 허용은 일시적이어야 하며 후속 정리 TODO를 남긴다

---

## 2) 아키텍처 원칙

### 2.1 기본 구조
- 내부 유스케이스: 레이어드 구조로 단순/명확하게
- 외부 I/O: 인터페이스 + 어댑터(헥사고날 스타일)로 분리

### 2.2 기술 교체 가능성
- Slack/LLM/DB/Search 등 외부 의존은 Adapter 경계 뒤에 배치
- 핵심 도메인 로직은 프레임워크/벤더 종속 최소화

### 2.3 예시
- `application/service` : 유스케이스 흐름
- `domain` : 비즈니스 규칙
- `adapter/inbound` : API/Command 진입
- `adapter/outbound` : DB/외부 API

---

## 3) 로그/관측성 규칙

### 3.1 기본
- 개발 친화 로그(debug/info/warn/error)
- 민감정보는 반드시 마스킹
- 에러 추적 가능한 컨텍스트 기록

### 3.2 고트래픽 구간
- 샘플링/집계 로그 허용
- 과도한 로그로 시스템 노이즈 유발 금지

### 3.3 권장 공통 필드
- `requestId`, `traceId`
- `userId`(가능 시)
- `endpoint`/`command`
- `elapsedMs`
- `errorCode`

---

## 4) 에러/응답 규격

- 에러코드 중심 설계
- 임의 응답 포맷 금지
- 성공/실패 포맷 일관성 유지

권장 형태:
```json
{ "data": { } }
```
```json
{ "error": { "code": "SOME_ERROR", "message": "..." } }
```

---

## 5) 테스트 전략

### 5.1 단계형 정책
- **초기 개발**: 핵심 + 경계/예외 테스트(B)
- **출시 전/연동 단계**: 통합/E2E 필수(C)

### 5.2 형식
- Given / When / Then 구조
- 실패 케이스를 happy path와 동등하게 취급

---

## 6) Git/PR/CI 규칙

### 6.1 브랜치 전략
- `main` 직접 작업 금지
- `develop` 기본 브랜치
- 기능은 `feat/*` 브랜치에서 개발
- 브랜치명은 `feat/actual-name-vN` 패턴을 사용
- `actual-name`은 영어 소문자/하이픈만 허용
- `vN`은 같은 기능 반복 작업 차수(`v1`, `v2`, `v3` ...)를 의미
- 날짜/이슈번호는 현재 필수 아님
- `develop` 직접 작업/커밋/푸시 금지(사용자 명시 승인 시에만 예외)
- 모든 변경은 기능 브랜치 기반 PR로만 반영

### 6.2 병렬 개발
- `git worktree` 기반 병렬 작업 권장
- 에이전트별 담당 영역 사전 선언
- 공통 파일 변경은 통합 담당 승인 필요

### 6.3 통합 방식
- 통합 전담 AI(Integrator) 운영
- 작은 PR 단위 + 자주 rebase
- 최종 통합 PR에서 구조/표준/충돌 정리

### 6.4 PR/CI
- PR 필수, AI 리뷰 필수
- CI(lint/typecheck/test) 통과 전 머지 금지
- 커밋/푸시 전 사용자 확인 필수
- 커밋 메시지는 Conventional Commits 사용
- **커밋 메시지/PR 제목/PR 본문은 한글 우선 작성**
- PR 본문에는 "무엇을 왜 개발했는지"를 상세히 작성하고 템플릿 항목을 최대한 채울 것

---

## 7) 문서화 정책 (강제)

### 7.1 기본 문서
- README
- ARCHITECTURE
- ADR(의사결정 기록)
- API/Command 문서
- RUNBOOK(운영)
- CHANGELOG(SemVer)

### 7.2 의사결정 기록
- "나중에 하자"도 backlog/decision log에 기록
- 문서 없는 결정은 없는 결정으로 간주

---

## 8) 기술 도입 심사 규칙 (강제)

신규 기술/라이브러리 도입 전 아래를 문서화:
1. 도입 필요성
2. 대안(최소 2개)
3. 트레이드오프
4. 동작 원리
5. 기술의 원래 목적
6. 유사 기술 비교

추가 규칙:
- 승인 없는 신규 의존성 도입 금지
- `latest` 버전 사용 금지 (명시 버전 고정)

---

## 9) 보안/비밀 관리

- `.env` 커밋 금지, `.env.example`만 커밋
- 토큰/키/개인정보 로그 출력 금지
- 서명 검증/권한 검증 누락 금지

---

## 10) 이식/백업 표준

백업은 반드시 세트로 관리:
1. 코드
2. DB/스토리지 데이터
3. 마이그레이션 버전
4. 환경변수 템플릿 + 시크릿 관리 경로
5. 런타임 버전 정보

목표:
- 다른 환경에서 동일 동작 재현
- 장애 후 빠른 재기동/복구

---

## 11) 금지 사항

- 읽기 어려운 과압축 코드
- 책임 섞인 비대 함수/비대 클래스
- `any` 남용
- 하드코딩된 환경 의존 경로/설정
- 문서/테스트 없는 무근거 변경
- 민감정보 로그 출력

---

## 12) 작업 체크리스트

### 시작 전
- 요구사항/가정/비목표 명시
- 변경 범위 정의
- 실패 시나리오 정의

### 종료 전
- 타입/린트/테스트 통과
- 로그/에러코드 점검
- 문서/ADR/CHANGELOG 업데이트
- 이식성 점검(환경 의존 제거)
