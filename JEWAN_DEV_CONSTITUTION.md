# JEWAN_DEV_CONSTITUTION.md

> 목적: 프로젝트 종류와 기술 스택이 바뀌어도 동일하게 적용되는 전역 개발 헌법.
> 원칙: 가독성, 추적성, 이식성, 확장성, 단일 책임.

---

## 0) 절대 원칙 (Non-negotiable)

1. **가독성 최우선**: 짧은 코드보다 읽기 쉬운 코드 우선.
2. **명시성 우선**: 파라미터/데이터 출처가 코드에서 즉시 보여야 함.
3. **SRP 강제**: 함수/클래스/모듈은 하나의 책임만 가짐.
4. **이식성 보장**: 환경이 바뀌어도 동작 동일성 유지.
5. **추적성 보장**: 모든 변경/결정/보류사항은 기록.
6. **확장성 우선 설계**: 외부 기술 교체 가능 구조 유지.

---

## 1) 코드 스타일 규칙

### 1.1 가독성/포맷팅
- 들쑥날쑥한 depth 금지 (중첩 최소화)
- 코드 정렬/줄바꿈/공백을 미적으로 일관되게 유지
- 한 눈에 흐름이 읽히는 구조 우선

### 1.2 제어 흐름
- Guard clause 우선
- `else` 최소화
- 긴 분기/반복은 함수 분리

### 1.3 네이밍 (엄격)
- 이름만 보고 역할/의도가 즉시 드러나야 함
- 함수명은 동사 시작, 도메인 용어 일관성 유지
- 약어 남용 금지
- 네이밍 규칙 위반은 PR 차단

### 1.4 SRP/역할 분리
- 함수 1개 = 책임 1개
- 서비스는 orchestration 책임만
- 유효성 검증은 validator 계층으로 분리
- 공통 검증 로직은 재사용 가능 컴포넌트로 관리

### 1.5 타입 안정성
- `any` 금지
- 입출력 타입 명시
- null/undefined/예외 흐름 명시적 처리

### 1.6 상수/중복
- 매직 넘버/스트링 상수화
- 중복 로직은 공통 모듈로 추출

---

## 2) 아키텍처 원칙

### 2.1 기본 구조
- 내부 유스케이스: 레이어드 구조로 단순/명확하게
- 외부 I/O: 인터페이스 + 어댑터(헥사고날 스타일)로 분리

### 2.2 기술 교체 가능성
- Slack/LLM/DB/Search 등 외부 의존은 Adapter 경계 뒤에 배치
- 핵심 도메인 로직은 프레임워크/벤더 종속 최소화

### 2.3 예시
- `application/service` : 유스케이스 흐름
- `domain` : 비즈니스 규칙
- `adapter/inbound` : API/Command 진입
- `adapter/outbound` : DB/외부 API

---

## 3) 로그/관측성 규칙

### 3.1 기본
- 개발 친화 로그(debug/info/warn/error)
- 민감정보는 반드시 마스킹
- 에러 추적 가능한 컨텍스트 기록

### 3.2 고트래픽 구간
- 샘플링/집계 로그 허용
- 과도한 로그로 시스템 노이즈 유발 금지

### 3.3 권장 공통 필드
- `requestId`, `traceId`
- `userId`(가능 시)
- `endpoint`/`command`
- `elapsedMs`
- `errorCode`

---

## 4) 에러/응답 규격

- 에러코드 중심 설계
- 임의 응답 포맷 금지
- 성공/실패 포맷 일관성 유지

권장 형태:
```json
{ "data": { } }
```
```json
{ "error": { "code": "SOME_ERROR", "message": "..." } }
```

---

## 5) 테스트 전략

### 5.1 단계형 정책
- **초기 개발**: 핵심 + 경계/예외 테스트(B)
- **출시 전/연동 단계**: 통합/E2E 필수(C)

### 5.2 형식
- Given / When / Then 구조
- 실패 케이스를 happy path와 동등하게 취급

---

## 6) Git/PR/CI 규칙

### 6.1 브랜치 전략
- `main` 직접 작업 금지
- `develop` 기본 브랜치
- 기능은 `feat/*` 브랜치에서 개발

### 6.2 병렬 개발
- `git worktree` 기반 병렬 작업 권장
- 에이전트별 담당 영역 사전 선언
- 공통 파일 변경은 통합 담당 승인 필요

### 6.3 통합 방식
- 통합 전담 AI(Integrator) 운영
- 작은 PR 단위 + 자주 rebase
- 최종 통합 PR에서 구조/표준/충돌 정리

### 6.4 PR/CI
- PR 필수, AI 리뷰 필수
- CI(lint/typecheck/test) 통과 전 머지 금지
- 커밋 메시지는 Conventional Commits 사용
- **커밋 메시지/PR 제목/PR 본문은 한글 우선 작성**
- PR 본문에는 "무엇을 왜 개발했는지"를 상세히 작성하고 템플릿 항목을 최대한 채울 것

---

## 7) 문서화 정책 (강제)

### 7.1 기본 문서
- README
- ARCHITECTURE
- ADR(의사결정 기록)
- API/Command 문서
- RUNBOOK(운영)
- CHANGELOG(SemVer)

### 7.2 의사결정 기록
- "나중에 하자"도 backlog/decision log에 기록
- 문서 없는 결정은 없는 결정으로 간주

---

## 8) 기술 도입 심사 규칙 (강제)

신규 기술/라이브러리 도입 전 아래를 문서화:
1. 도입 필요성
2. 대안(최소 2개)
3. 트레이드오프
4. 동작 원리
5. 기술의 원래 목적
6. 유사 기술 비교

추가 규칙:
- 승인 없는 신규 의존성 도입 금지
- `latest` 버전 사용 금지 (명시 버전 고정)

---

## 9) 보안/비밀 관리

- `.env` 커밋 금지, `.env.example`만 커밋
- 토큰/키/개인정보 로그 출력 금지
- 서명 검증/권한 검증 누락 금지

---

## 10) 이식/백업 표준

백업은 반드시 세트로 관리:
1. 코드
2. DB/스토리지 데이터
3. 마이그레이션 버전
4. 환경변수 템플릿 + 시크릿 관리 경로
5. 런타임 버전 정보

목표:
- 다른 환경에서 동일 동작 재현
- 장애 후 빠른 재기동/복구

---

## 11) 금지 사항

- 읽기 어려운 과압축 코드
- 책임 섞인 비대 함수/비대 클래스
- `any` 남용
- 하드코딩된 환경 의존 경로/설정
- 문서/테스트 없는 무근거 변경
- 민감정보 로그 출력

---

## 12) 작업 체크리스트

### 시작 전
- 요구사항/가정/비목표 명시
- 변경 범위 정의
- 실패 시나리오 정의

### 종료 전
- 타입/린트/테스트 통과
- 로그/에러코드 점검
- 문서/ADR/CHANGELOG 업데이트
- 이식성 점검(환경 의존 제거)
